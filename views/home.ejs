<%- include('partials/header') -%>
 
    <h1><%= title %>.</h1>
    <p><%= para %></p>

    <% posts.forEach(function (post) {       %>
    <h1> <%= post.name %> </h1>
    <p> <%= post.content.substring(0,100) + "..." %> <a href="/posts/<%= post.name %>" >Read more</a> </p>
    <% })   %>


<%- include('partials/footer') -%>
